[phases.setup]
nixPkgs = ["php82", "php82Packages.composer"]

[phases.build]
cmds = ["echo 'No build step needed for PHP native'"]

[phases.start]
cmd = "php -S 0.0.0.0:8080 -t ."

[variables]
PHP_VERSION = "8.2"